name: trabajo_sumativo_3
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  
  sdk: '>=3.0.0-322.0.dev <4.0.0'
  

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^1.20.0
  firebase_auth: ^3.11.2

  http: ^0.13.6

  provider: ^6.0.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/